import React from 'react';

const CompanyDetails = ({ company }) => {
  return <p>This should show the jobs at the given company</p>;
};

export default CompanyDetails;

/* <h3>{company.handle}</h3>
            <h3>Employees: {company.numEmployees}</h3> */
